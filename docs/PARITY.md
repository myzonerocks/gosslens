# Parity

Every capability ships on all three platforms or it does not ship, and the
conformance harness keeps this table honest. The public APIs are idiomatic
per platform but share the same shapes: Session, LensRegistry,
CaptureOutput, Events.

| Capability | iOS | Android | Web |
|------------|-----|---------|-----|
| Live capture | demonstrated on device | built, no hardware yet | demonstrated in browser |
| Preview render | demonstrated on device | built, no hardware yet | demonstrated in browser |
| Face tracking | demonstrated on device | built, no hardware yet | demonstrated in browser |
| Segmentation | proven in the host harness, multiclass with per-class lens channels | built, no hardware yet | demonstrated in browser, the sync segmentation core on the wasm tracking module, subject and per-class channels fed back through set_segmentation_mask and set_segmentation_class_mask |
| Pose tracking | proven in the host harness | built, no hardware yet | demonstrated in browser, the pose pipeline on the wasm tracking module, read through a GossPoseTracker |
| Hand tracking (landmarks, handedness, gestures) | proven in the host harness | built, no hardware yet | demonstrated in browser, the hand pipeline on the wasm tracking module, read through a GossHandTracker |
| Multi-person face and body results (submit, count, per-index read-back) | proven in the host harness: a face-anchored model fans out to every submitted face and a body-anchored model to every submitted body, with the kept count read back each frame | built, no hardware yet | built |
| Beauty effects | demonstrated on device | built, no hardware yet | demonstrated in browser |
| Lens runtime | demonstrated on device | built, no hardware yet | demonstrated in browser, beauty-baseline only |
| Photo capture (deterministic PNG) | proven in the host harness | built, no hardware yet | engine path not wired; the web SDK exports a PNG off the composited canvas (captureFrame) |
| Video recording | proven in the host harness on the Apple encoder | built on MediaCodec, no hardware yet | engine encoder native-only; the web SDK records through the browser MediaRecorder (captureStream + engine-normalized policy) |
| Photo formats (JPEG built-in, HEIC platform) | JPEG and HEIC proven in the host harness | JPEG from the engine's own encoder, built; HEIC backend not built | JPEG from the engine's own encoder, built; HEIC backend not built |
| Color-managed stills (Display-P3/Rec2020 tags, 16-bit PNG) | proven in the host harness: wide-gamut PNG carries cHRM/gAMA and JPEG an ICC, sRGB carries neither, 16-bit PNG reports bit depth 16 | core encoder, every target | core encoder, every target |
| High-resolution still capture (decoupled from preview, supersampled) | proven in the host harness: full-sensor size, anti-aliased supersampling, deterministic | Kotlin wrapper built, no hardware yet | web wrapper built over the wasm core |
| Tiled still composition (2D + 3D sub-frustum, streamed) | proven in the host harness: 2D grids byte-identical to a single target; 3D content tiles past the texture cap (mesh byte-identical, particles within a sub-pixel); the tiled PNG streams a band at a time, peak heap a full render buffer below the full-buffer path | Kotlin wrapper built, no hardware yet | web wrapper built over the wasm core |
| Audio triggers (level, beat) | proven in the host harness | built, no hardware yet | not wired |
| Recording audio track + A/V sync | proven in the host harness, zero end drift | video-only until the audio encoder is built | backend not built |
| Multi-source composition (layout grids, per-source blend and chroma-key, screen-share) | proven in the host harness: a side-by-side layout composites the camera left and a named source right, a source at half opacity blends over the camera rather than overwriting it, and a screen-share source letterboxes to its cell, each deterministic | built, no hardware yet | built |
| World tracking (pose, planes, anchors, light) | ARKit source built, no hardware yet; seam proven on the replay track in the host harness | ARCore demo feeder built, no hardware yet | WebXR source built and typechecked, no browser run yet |
| Geofilters (location-gated lenses; circle, box, polygon and named fences, accuracy gate) | proven in the host harness: a geo.in_region trigger fires inside a circle, box or polygon region and not outside, an accuracy gate refuses a vague fix, and only the in-region boolean crosses the trigger rail so the location never leaves the engine; named circular regions a lens fires by name sit alongside the default fence, wrapped on every SDK | built, no hardware yet | built |
| Lens physics (rigid bodies, collider shapes, mesh + head colliders, grab/throw, world-mesh) | proven in the host harness: box, sphere, cylinder, capsule, convex-hull and concave-mesh colliders (a hull built from a glTF among them) settle deterministically, a kinematic capsule tracks the head pose so a marker bounces off the tracked head, grab and throw drives a body, and a body rests on a world-anchored mesh; each with a reference lens and a proof | built, Jolt on the NDK, no hardware yet | built, Jolt on wasm, single-threaded |
| Lens physics joints and jiggle (distance, point, fixed, hinge, spring; chain secondary motion) | proven in the host harness: each joint type constrains deterministically and a jiggle chain lags a swept anchor with secondary motion, with a reference lens and a proof | built, Jolt on the NDK, no hardware yet | built, Jolt on wasm, single-threaded |
| Lens cloth (soft-body sheets) | proven in the host harness, deterministic drape | built, Jolt on the NDK, no hardware yet | built, Jolt on wasm, single-threaded |
| Lens 2D physics world (planar rigid bodies, colliders, springs, SPH fluid) | proven in the host harness: planar bodies stay in the z plane where 3D bodies slide off, a 2D spring stretches in-plane, and an SPH fluid pools, each with a reference lens and a proof | built, Jolt on the NDK, no hardware yet | built, Jolt on wasm, single-threaded |
| Lens strand hair (Jolt compute) | proven in the host harness, deterministic settle | built, Jolt on the NDK, no hardware yet | built, Jolt on wasm, single-threaded |
| Analytic two-bone IK (goss_solve_two_bone_ik) | proven in the host harness: the solver reaches the target and holds both bone lengths, bends the mid joint toward the pole, and extends straight when the target is out of reach, deterministically | built, no hardware yet | built |
| Lens scripting (QuickJS-ng, sandboxed, deterministic) | proven in the host harness: a script drives a parameter from a signal, bit-stable | built, QuickJS-ng linked, no hardware yet | built, QuickJS-ng linked for wasm |
| Event and trigger interactivity (host-fired events, signal-driven triggers) | proven in the host harness: a host-fired event fires a lens trigger for exactly one tick and ignores non-matching names, and a trigger fires only while the world pose is inside a device volume, each bit-stable | built, no hardware yet | built |
| Lens audio playback (miniaudio, deterministic mixer) | proven in the host harness: a play_sound trigger mixes a voice, silent before, bit-stable after | built, miniaudio linked, no hardware yet | built, miniaudio linked for wasm |
| Post-effect nodes (blur, parametric grade, bloom) | proven in the host harness, each with a reference lens and a conformance proof | built, no hardware yet | built |
| Depth post-passes (depth-of-field, depth fog, depth-edge outline) | proven in the host harness: a depth gradient blurs off the focus plane and hazes the far side, a depth step draws a toon outline, each with a reference lens and a proof | built, no hardware yet | built |
| 3D-photo depth warp and monocular depth (parallax.pass sways layers by the scene depth's distance from a focus plane as the device tilts; an ml.infer depth binding lets an author depth net feed that same depth rail with no sensor) | proven in the host harness: over a submitted three-band depth the near band shifts while the focus holds and reverses with the tilt, and a bundled depth net's output becomes the session depth with none submitted and drives the warp, each with a proof | built, no hardware yet | built |
| Motion trail (temporal echo of the previous frame) | proven in the host harness: the frame right after a scene cut still carries the previous frame's echo, with a reference lens and a proof | built, no hardware yet | built |
| Screen-space reflection (depth-gated reflective floor) | proven in the host harness: a near depth wets the floor with a mirrored reflection where a far one stays dry, with a reference lens and a proof | built, no hardware yet | built |
| Procedural sky dome behind the foreground (pose-driven env.pass) | proven in the host harness: tilting the submitted camera pose pans the sky behind the segmented subject, with a reference lens and a proof | built, no hardware yet | built |
| Equirect environment map (image env.pass sampled by pose) | proven in the host harness: yawing the camera pans a shipped equirect panorama behind the segmented subject, with a reference lens and a proof | built, no hardware yet | built |
| Material graph shaders (node-graph fragment shaders lowered to GLSL) | proven in the host harness: authored graphs compile and render, with reference lenses and a conformance proof | built, no hardware yet | built |
| glTF animation mixer and morph targets (weights bound to parameters) | proven in the host harness: clip and morph weights blend the pose, each with a reference lens and a proof | built, no hardware yet | built |
| Model lighting (opt-in directional light and sky-hemisphere ambient shade a lens's glTF model nodes, with a Blinn-Phong specular highlight and emissive self-illumination, on static, morphing, and skinned meshes) | proven in the host harness: a sphere lit from the front shades differently than the same sphere lit from the back and both differ from the unlit sphere, a morphing mesh lights through the lit dynamic path where it drew flat before, and an emissive material self-illuminates over the lit diffuse, each with a proof | built, no hardware yet | built |
| 2D sprite and text nodes (image overlay, built-in-font text) | proven in the host harness, each with a reference lens and a proof | built, no hardware yet | built |
| 2D + AR brush and persistent world board (pen, highlighter, marker, neon; erase, undo, redo) | proven in the host harness: a screen stroke builds a bounded triangle ribbon the renderer pulls, with every mode, erase, undo, redo and clear, allocation-free; the world-anchored AR brush and persistent board build on the same ribbon and carry core proofs | built, no hardware yet | built |
| Animated GIF video textures (sprite plays a decoded clip) | proven in the host harness: a sprite decodes a shipped GIF and advancing the lens clock draws a different frame, with a reference lens and a proof | built, no hardware yet | built |
| MP4 video textures (video.texture node streams a clip off the hardware decoder) | proven in the host harness: a recorded clip decodes back onto a sprite through AVAssetReader and advances off the frame clock, bit-stable, with a reference lens and a proof | deterministic stub; AMediaCodec decode queued | deterministic stub; browser decode queued |
| Bring-your-own ML models (ml.infer node: a bundled TFLite or ONNX net drives lens parameters, a subject mask, or a restyle sprite) | proven in the host harness: bundled TFLite and ONNX nets drive a parameter, a segmentation channel, a placed sprite, and a restyle image, each with a proof | built on the inference rail | built on the inference rail |
| Inference delegate scheduling (platform accelerator first, XNNPACK CPU fallback) | resolved per target and unit-tested per platform; host and conformance run on XNNPACK | CoreML on Apple / NNAPI on Android linked into the device build | XNNPACK CPU on wasm |
| On-device generative restyle (diffusion img2img and text-to-image, greenscreen and full-face masks, optical-flow temporal coherence, text-to-material into the face mesh and the shader material graph, text-to-3D splat cloud and mesh surface) | proven in the host harness: each generative node runs its bundled models off the frame thread and draws through a sprite, mesh, material graph, point cloud, or surface, with a proof apiece | built on the inference rail | built on the inference rail |
| Prompt-to-lens compiler (goss_compile_prompt turns a text prompt into a GLF manifest on device) | proven in the host harness: the op emits a manifest that activates as a lens and renders, plus compiler unit tests | built | built |
| Avatars (a selfie avatar generated from a submitted still as a colored splat cloud or a mesh surface, head reenactment from an injected source performance, an audio-driven talking avatar, expression retargeting off the blendshapes and head pose, a stylized avatar, monocular mocap to a rigged body) | proven in the host harness: the selfie op generates the avatar off the live camera, an injected jaw reenacts and deforms the retarget mesh, a toon avatar stays live, the mocap palette rotates each bone, each with a proof | selfie op bound over JNI (NV12); retarget, talk, stylize, mocap ride the lens runtime | selfie op bound over wasm (RGBA); retarget, talk, stylize, mocap ride the lens runtime |
| Anisotropic gaussian splat render (a splat.cloud in gaussian mode draws each splat's 3D covariance as an oriented screen ellipse, depth-sorted and composited with a premultiplied opacity over-blend; placement composites it over the frame, behind the segmented subject as a 3D backdrop, or inside a portal rect) | proven in the host harness: an x-elongated covariance reads wider than tall and a y-elongated one taller than wide, a near opaque splat composites over a far one and swaps with the depth, a portal confines the cloud to its rect, and a background places it behind the subject, each with a proof | built on the render path | built on the render path |
| Guided capture and deterministic reconstruction (goss_session_capture_view steers a scan across target viewpoints and back-projects each posed depth frame into a gaussian cloud on device, reproducibly; a splat.cloud with source:reconstruction draws that cloud live) | proven in the host harness: a ring of eight yaw viewpoints completes the coverage and reconstructs 2048 gaussians landing where the poses and depth put them, identical across a rescan, and a source:reconstruction cloud holds the frame with no scan then draws the cloud once scanned, each with a proof | capture ops bound over JNI | capture ops bound over wasm |
| Lens particles (deterministic CPU sim, GPU points, GPU-compute path, GPU mesh instancing) | proven in the host harness: the CPU fountain falls and a GPU-compute fountain tracks it under the same forces, particle meshes draw instanced, settled differs from initial, bit-stable across runs | pure-Zig CPU sim on device; GPU-compute path built | pure-Zig CPU sim in browser; GPU-compute path built |
| Enhance post-passes (dehaze, glare recovery, directional relight with studio presets, radial vignette, low-light lift, auto white balance, masked grade, specular dereflect, subject-to-background harmonize, content-aware inpaint) | proven in the host harness: each pass lifts, corrects, recolors, or fills its target and holds the frame through without its input, each with a reference lens and a proof | built, no hardware yet | built |
| Camera-geometry passes (lens undistort from submitted intrinsics, electronic stabilization, rolling-shutter correction from the orientation stream, horizon leveling, region zoom, gaze redirection, auto-framing) | proven in the host harness: undistort straightens a barrel grid, stabilize steadies a jitter, rolling-shutter and roll-lock read the orientation stream, gaze and auto-frame ride the tracked face, each with a proof | built, no hardware yet | built |
| Multi-frame fusion (temporal.fuse: denoise averaging, frame interpolation by phase, HDR exposure-bracket merge) | proven in the host harness: a ring of the last N frames averages to their mean, an interpolate net tracks the phase between two frames, and an exposure bracket fuses once the whole set lands, each with a proof | built on the inference rail | built on the inference rail |
| On-device speech (audio.infer: ASR captions, speaker diarization, live translation, dubbing, and a voice.command trigger) | proven in the host harness: a captioning net decodes the mic window, a diarizer clusters speakers, a translator decodes into a caption, a dub net synthesizes speech into the mixer, and a voice.command trigger fires a lens action when the recognized speech carries its phrase, each with a proof | native mic feeds the rail | wasm mic feeds the rail |
| Microphone cleanup and voice conversion (audio.enhance noise suppression, de-reverberation, and echo cancellation, voice.transform pitch shift, pitch-independent formant shift, and a robot ring-modulator), deterministic, no model | proven in the host harness: a tone under per-sample hiss loses its high-frequency energy, a room reverb and a room echo each lose their autocorrelation peak at their delay under an inverse comb, the voice pitch shifts while its duration holds, a cepstral formant warp moves a spectral-envelope peak while the pitch stays put, and a robot carrier ring-modulates a tone into its sidebands, each with a proof | native mic rail | wasm mic rail |
| Music identification (on-device acoustic fingerprint and catalog match, model-free) | proven in the host harness: a two-second noisy snippet from the middle of a registered melody identifies it over a decoy by the time offset its spectral-peak landmarks share, while unrelated audio stays below threshold, with a proof and fingerprinter unit tests | built, catalog on the engine | built, catalog on the engine |

"Demonstrated" means executed on the real target through the public
path; "built" means the code exists and compiles but no physical device
has run it yet. Rows appear as capabilities land. World tracking always comes from the
platform, ARKit on iOS, ARCore on Android, WebXR in the browser, behind one
core interface. A lens that wants world data falls back to a defined preview
behavior when tracking is unavailable.
