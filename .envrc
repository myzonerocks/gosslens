# With Nix on the machine, direnv enters the flake's shell on cd; without it nothing changes.
# Flakes are turned on for this evaluation whatever nix.conf says.
export NIX_CONFIG="experimental-features = nix-command flakes"
if has nix; then
  watch_file nix/devShell.nix
  use flake
fi
